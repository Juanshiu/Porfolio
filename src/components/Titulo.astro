---
const {title, svg, class: className} = Astro.props

---
    <h2 class={` ${className} relative text-3xl font-semibold mb-6 flex gap-x-3 items-center text-transparent `}>
        <slot/>
        {title}
    </h2>
